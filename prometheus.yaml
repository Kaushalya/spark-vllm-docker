global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/llm-rules.yaml

scrape_configs:
  - job_name: vllm
    static_configs:
      - targets:
          - host.docker.internal:8000

  - job_name: sglang
    static_configs:
      - targets:
          - host.docker.internal:30000
